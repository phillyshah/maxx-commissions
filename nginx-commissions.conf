# ⚠️ LEGACY — NOT USED IN PRODUCTION.
# The server uses Traefik (see deploy/traefik-commissions.yml), not nginx.
# Do not install this; nginx will fail to bind port 80 (Traefik owns it).
server {
    listen 80;
    server_name commissions.phillyshah.com;

    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }

    location /static/ {
        alias /opt/commission-app/static/;
        expires 7d;
    }
}
